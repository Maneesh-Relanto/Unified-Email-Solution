# Deployment Configuration

This folder contains deployment and hosting configuration files.

## Files

- **netlify.toml** - Netlify hosting configuration for serverless deployment

## Usage

When deploying to Netlify, you may need to specify the config path:
```bash
netlify deploy --config deployment/netlify.toml
```

Or link to this file in your Netlify dashboard settings.
